name: lite_storage
description: "A super-duper-mega-ultra-extra fast, light and synchronous key-value storage written."
version: 1.0.0
homepage: ''

environment:
  sdk: '^3.7.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  path_provider:

dev_dependencies:
  flutter_lints: